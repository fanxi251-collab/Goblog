# 服务器配置
server:
  host: "0.0.0.0"
  port: 8080

# 后台配置
admin:
  path: "/admin-panel-7x9k2m3p"   # 后台路径（复杂随机路径）
  username: "Fantasy"              # 初始用户名
  password: "746834335"            # 初始密码（生产环境请修改）

# 数据库配置
database:
  type: "postgres"
  host: "localhost"
  port: 5432
  user: "postgres"
  password: "523"
  dbname: "blog"
  sslmode: "disable"

# 文件上传配置
upload:
  path: "web/static/uploads"
  max_size: 10                # MB

# XSS清洗配置
xss:
  enabled: true

# 评论/留言配置
comment:
  rate_limit: 3                    # 频率限制（秒），同IP/Token间隔
  auto_approve: true               # 是否自动审核通过（无敏感词时）
  blocked_words:                  # 敏感词列表（触发直接拒绝）
    - "fuck"
    - "shit"
    - "草泥马"
    - "去你妈"
  admin_emails:                   # 管理员邮箱（不可注册）
    - "746834335@qq.com"